{
  "name": "@webos/desktop",
  "version": "0.0.1beta",
  "description": "WebOS Desktop UI - TypeScript + WebGPU desktop environment",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
